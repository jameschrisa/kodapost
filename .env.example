# =============================================================================
# NostalgiaFlow — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your keys:
#   cp .env.example .env.local
#
# IMPORTANT: Never commit .env.local to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Anthropic API (for text generation and image analysis)
# -----------------------------------------------------------------------------
# Used by Claude to analyze uploaded photos and generate text overlays
# for each carousel slide (headlines, subtitles, CTAs).
#
# Get your API key at: https://console.anthropic.com/settings/keys
# Docs: https://docs.anthropic.com/en/docs/initial-setup
#
# NOTE: We use NOSTALGIA_ANTHROPIC_KEY (not ANTHROPIC_API_KEY) to avoid
# conflicts with development tools that set their own ANTHROPIC_API_KEY.
NOSTALGIA_ANTHROPIC_KEY=

# -----------------------------------------------------------------------------
# Application URL
# -----------------------------------------------------------------------------
# The public-facing URL of your app. Used for generating share links
# and OAuth callback URLs. No trailing slash.
#
# For local development, leave as-is. For production, set to your domain.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Social Media OAuth — Instagram (Meta Graph API)
# -----------------------------------------------------------------------------
# Required to post carousel images to Instagram.
# Create an app at: https://developers.facebook.com/apps/
# Enable Instagram Graph API and add Instagram Content Publishing permission.
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=

# -----------------------------------------------------------------------------
# Social Media OAuth — TikTok (Content Posting API)
# -----------------------------------------------------------------------------
# Required to post photo carousels to TikTok.
# Register your app at: https://developers.tiktok.com/
# Enable Content Posting API access.
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Social Media OAuth — LinkedIn (Posts API)
# -----------------------------------------------------------------------------
# Required to post document carousels to LinkedIn.
# Create an app at: https://www.linkedin.com/developers/apps/
# Request w_member_social permission.
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Social Media OAuth — YouTube (Google Cloud / YouTube Data API v3)
# -----------------------------------------------------------------------------
# Required to post community posts to YouTube.
# Create an app at: https://console.cloud.google.com/
# Enable YouTube Data API v3 and configure OAuth consent screen.
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Social Media OAuth — Reddit (Reddit API)
# -----------------------------------------------------------------------------
# Required to post gallery posts to Reddit.
# Register your app at: https://www.reddit.com/prefs/apps/
# Select "web app" type with redirect URI.
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Social Media OAuth — Lemon8 (Lemon8 Content API)
# -----------------------------------------------------------------------------
# Required to post photo carousels to Lemon8.
# Register your app at the Lemon8 developer portal.
LEMON8_CLIENT_KEY=
LEMON8_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Music Library — Jamendo API
# -----------------------------------------------------------------------------
# Used by the Music Library in the AudioPanel to search Creative Commons
# licensed tracks from Jamendo's catalog (600K+ tracks).
#
# Register free at: https://devportal.jamendo.com/signup
# Your Client ID appears on the developer portal dashboard after sign-up.
# Free for non-commercial use. No hard rate limit (soft cap at 500K hits).
#
# Note: Audius integration works without any API key.
JAMENDO_CLIENT_ID=

# -----------------------------------------------------------------------------
# Token Encryption Key
# -----------------------------------------------------------------------------
# Used to encrypt OAuth access tokens stored in cookies.
# Generate with: openssl rand -hex 32
TOKEN_ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# Clerk Authentication
# -----------------------------------------------------------------------------
# Clerk handles user authentication, sign-in/sign-up flows, SSO, and
# user profile management. When these keys are set, the app requires
# authentication; when unset, the app runs without auth.
#
# Get your keys at: https://dashboard.clerk.com
# Docs: https://clerk.com/docs
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Clerk route configuration (defaults below are recommended)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# Clerk Webhook Secret (for user.created webhook → sets default trial metadata)
# Set up in Clerk Dashboard → Webhooks → Add Endpoint → Subscribe to user.created
# URL: https://your-domain.com/api/webhooks/clerk
CLERK_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Telegram Bot Integration
# -----------------------------------------------------------------------------
# The KodaPost Telegram Bot lets users create carousels through a conversational
# flow directly in Telegram. Photos are sent to the bot, which guides the user
# through story, vibes, caption, and generation — then shares a preview link.
#
# Step 1: Open Telegram and message @BotFather
# Step 2: Send /newbot and follow the prompts to name your bot
# Step 3: Copy the bot token here
# Step 4: Deploy and call POST /api/telegram/setup to register the webhook
#
# Docs: https://core.telegram.org/bots#botfather
TELEGRAM_BOT_TOKEN=

# Webhook secret for verifying incoming Telegram updates.
# Generated automatically during POST /api/telegram/setup — copy it here.
TELEGRAM_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Headless API — Database (Turso / libSQL)
# -----------------------------------------------------------------------------
# Turso database URL. For local dev: file:local.db
# For Turso cloud: libsql://your-db-name-org.turso.io
TURSO_DATABASE_URL=file:local.db

# Turso auth token. Leave blank for local file-based SQLite.
# For Turso cloud: generate at https://turso.tech/dashboard
TURSO_AUTH_TOKEN=

# -----------------------------------------------------------------------------
# Headless API — Admin Secret
# -----------------------------------------------------------------------------
# Master secret for creating API keys via POST /api/v1/keys.
# Generate with: openssl rand -hex 32
API_ADMIN_SECRET=
